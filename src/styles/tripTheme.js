import { Colors as GlobalColors, Spacing as GlobalSpacing, Fonts as GlobalFonts } from './globalStyles';

export const Colors = {
  ...GlobalColors,
  black: '#000000',
  white: '#FFFFFF',
  textPrimary: GlobalColors.textPrimary,   
  textSecondary: GlobalColors.textSecondary,
};

export const Spacing = GlobalSpacing;

export const Fonts = GlobalFonts;